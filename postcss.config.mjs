/** @type {import('postcss-load-config').Config} */
const config = {
  plugins: {
    tailwindcss: {},
    autoprefixer: {}, // autoprefixer is correctly configured here
  },
};

export default config;
